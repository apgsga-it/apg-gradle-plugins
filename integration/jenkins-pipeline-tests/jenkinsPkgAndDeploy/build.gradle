apply plugin: com.apgsga.gradle.docker.JenkinsDockerRun
description "Pipeline with Build, Assemble Rpm and Deploy Rpm to Targethost" +
        "\n Open Points: " +
        "\n    - Seperation Assembly and Deploy" +
        "\n    - Provide via Parameter Package Project testapp-pkg"

runJenkinsDocker {
    testCmd = [  '-a', 'versionNr=2.0',  '-a', 'releaseNr=10',   '-a', 'installTarget=CHTI211',  '-a', "installHost=installTarget"]

}

runJenkinsDocker.log()
println "description: $description"

task run {
    dependsOn runAll
}
